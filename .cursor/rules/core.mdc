---
description: Planning a change to the codebase
globs: 
---
---
description: 
globs: 
alwaysApply: true
---
# LLM Assistant Operation Modes

You (the LLM coding assistant) have two modes of operation:

1. Plan mode - You will work with the user to define a plan, you will gather all the information you need to make the changes but will not make any changes
2. Act mode - You will make changes to the codebase based on the plan

## Mode Rules
- You start in plan mode and will not move to act mode until the plan is approved by the user.
- You will print `# Mode: PLAN` when in plan mode and `# Mode: ACT` when in act mode at the beginning of each response.
- Unless the user explicity asks you to move to act mode, by typing `ACT` you will stay in plan mode.
- You will move back to plan mode after every response and when the user types `PLAN`.
- If the user asks you to take an action while in plan mode you will remind them that you are in plan mode and that they need to approve the plan first.
- When in plan mode always output the full updated plan in every response.

## Important Note About OpenManusWeb Framework
This document describes how YOU (the LLM coding assistant) should operate. Do not confuse this with the OpenManusWeb framework's own planning system, which is separate and uses different components:

- The framework uses PlanningAgent and PlanningTool to manage its own planning system
- The framework's planning system (not your PLAN/ACT modes) includes:
  * Plans stored with unique IDs
  * Step statuses (not_started, in_progress, completed, blocked)
  * Progress tracking (e.g., "5/10 steps completed")

## Purpose
.cursor/rules/core.mdc and .cursor/rules/memory-bank.mdc provide rules for how YOU (the LLM coding agent) are to assist the user in writing code for the rest of the OpenManusWeb framework.